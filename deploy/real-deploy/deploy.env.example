# 真实环境部署参数（deploy.sh 读取）。复制为 deploy.env 并按实际值修改；本文件只放非敏感值。

# 面板镜像引用（与推送的 tar 一致；ctr 导入后短名即可命中）
IMAGE="controlcenter-panel:0.1.0-deploy.1"

# 后端公网域名（Cloudflare Tunnel 指向本服务的 hostname，如 cc.example.com）
PANEL_PUBLIC_BASE_URL="https://cc.example.com"

# 前端来源（Cloudflare Pages，逗号分隔多个；写入 CORS 允许来源）
# 例：https://controlcenter.pages.dev,https://panel.example.com
CORS_ALLOWED_ORIGINS="https://controlcenter.pages.dev"

# 会话 Cookie SameSite：前端与后端不同站（*.pages.dev）填 None（自动加 Secure）；
# 前端最终落在与后端同站的自定义域（如 panel.example.com 与 cc.example.com）时建议改回 Lax
COOKIE_SAMESITE="None"

# 可选：集群外验证用（留空则跳过该步验证提示）
# TUNNEL_HOSTNAME=cc.example.com
